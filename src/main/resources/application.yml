server:
  port: 9600

spring:
#  datasource:
#    url: jdbc:mysql://localhost:3306/insurance_db?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
#    username: root
#    password: 123456
#    driver-class-name: com.mysql.cj.jdbc.Driver
  datasource:
    url: jdbc:mysql://10.158.96.40:3306/db_fcg?useSSL=false&serverTimezone=UTC
    username: root
    password: Jm3!gTz8#Rw9XpQ2
    driver-class-name: com.mysql.cj.jdbc.Driver


  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

mybatis-plus:
  # MyBatis-Plus 全局配置
  configuration:
    # ✔ 默认开启驼峰映射 (无需配置，但手动写上也没问题)
    map-underscore-to-camel-case: true
    # 打印 SQL
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

  global-config:
    db-config:
      # 主键策略：雪花算法（也可以用 AUTO 或 ASSIGN_ID）
      id-type: ASSIGN_ID

logging:
  level:
    com.tao.insurance: debug
